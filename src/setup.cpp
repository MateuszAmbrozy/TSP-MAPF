#include "../lib/setup.h"

const std::vector<std::pair<int, int>> setup::moves{{0, -1}, {0, 1}, {-1, 0}, {1, 0}};
const unsigned int setup::max_time = 1000;
const unsigned int setup::maxTasks = 1;
const unsigned int setup::maxCapacity = 1;
const unsigned int setup::maxStopTime = 1;
const unsigned int setup::cellSize = 20;
